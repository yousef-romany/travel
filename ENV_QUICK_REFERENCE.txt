╔══════════════════════════════════════════════════════════════════════════╗
║           ENVIRONMENT VARIABLES - QUICK REFERENCE                        ║
╚══════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────┐
│ QUICK SETUP (5 minutes)                                                  │
└──────────────────────────────────────────────────────────────────────────┘

1. Create .env file:
   $ npm run env:setup

2. Edit .env with your values:
   $ nano .env    # or use your editor

3. Validate configuration:
   $ npm run env:check

┌──────────────────────────────────────────────────────────────────────────┐
│ REQUIRED VARIABLES                                                       │
└──────────────────────────────────────────────────────────────────────────┘

Variable                          Get From                              
───────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_STRAPI_URL           Your Strapi URL                       
STRAPI_HOST                      Same as STRAPI_URL                    
NEXT_PUBLIC_STRAPI_TOKEN         Strapi → Settings → API Tokens        
NEXT_PUBLIC_INSTAGRAM_TOKEN      Facebook Developers → Your App        
NEXT_PUBLIC_WHATSAPP_NUMBER      Your WhatsApp Business Number         

┌──────────────────────────────────────────────────────────────────────────┐
│ OPTIONAL VARIABLES (But Recommended)                                     │
└──────────────────────────────────────────────────────────────────────────┘

Variable                          Get From                              
───────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_GA_MEASUREMENT_ID    Google Analytics → Data Streams       
CLOUDFLARE_ZONE_ID               Cloudflare → Overview → API           
CLOUDFLARE_API_TOKEN             Cloudflare → My Profile → API Tokens  

┌──────────────────────────────────────────────────────────────────────────┐
│ USEFUL COMMANDS                                                          │
└──────────────────────────────────────────────────────────────────────────┘

Setup:
  npm run env:setup              Create .env from template
  npm run env:check              Validate all variables
  npm run env:check:cloudflare   Check only Cloudflare variables

Development:
  npm run dev                    Start dev server
  npm run build:safe             Build with security checks
  npm run build:deploy           Build + verify + Cloudflare purge

Security:
  npm run security:check         Full security audit
  npm run security:cleanup       Clean up compromised files

┌──────────────────────────────────────────────────────────────────────────┐
│ COMMON ISSUES & SOLUTIONS                                                │
└──────────────────────────────────────────────────────────────────────────┘

Problem: "Missing required environment variable"
Solution: Run 'npm run env:check' to see which variables are missing

Problem: "Cloudflare configuration incomplete"
Solution: Set both ZONE_ID and API_TOKEN, or remove both

Problem: Changes not reflected
Solution: Restart dev server (Ctrl+C, then 'npm run dev')

Problem: Token expired (Instagram)
Solution: Regenerate token from Facebook Developers

┌──────────────────────────────────────────────────────────────────────────┐
│ WHERE TO GET API KEYS                                                    │
└──────────────────────────────────────────────────────────────────────────┘

Strapi Token:
  1. Login to https://dashboard.zoeholidays.com
  2. Settings → API Tokens → Create new API Token
  3. Copy token (you won't see it again!)

Instagram Token:
  1. Go to https://developers.facebook.com/apps
  2. Select your app → Instagram Graph API
  3. Generate Access Token
  4. Copy token

Cloudflare:
  1. Login to https://dash.cloudflare.com
  2. Zone ID: Overview → API section
  3. API Token: My Profile → API Tokens → Create Token

Google Analytics:
  1. Login to https://analytics.google.com
  2. Admin → Data Streams
  3. Copy Measurement ID (starts with G-)

┌──────────────────────────────────────────────────────────────────────────┐
│ SECURITY CHECKLIST                                                       │
└──────────────────────────────────────────────────────────────────────────┘

✅ .env file is in .gitignore (never commit it!)
✅ Different keys for development and production
✅ API keys stored in password manager
✅ Rotate keys every 3-6 months
✅ Use hosting platform secrets for production
✅ Never log environment variables
✅ Validate before deployment: npm run env:check

┌──────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                          │
└──────────────────────────────────────────────────────────────────────────┘

Check .env file exists:
  $ ls -la .env

Validate all variables:
  $ npm run env:check

View environment (safe - hides secrets):
  $ npm run env:check

Restart dev server:
  $ # Press Ctrl+C
  $ npm run dev

Clear Next.js cache:
  $ rm -rf .next
  $ npm run dev

┌──────────────────────────────────────────────────────────────────────────┐
│ DOCUMENTATION                                                            │
└──────────────────────────────────────────────────────────────────────────┘

📖 Complete Guide:   ENV_SETUP_GUIDE.md
📖 Security Guide:    SECURITY.md
📖 Cloudflare Setup:  CLOUDFLARE_SETUP.md
📖 Quick Start:       QUICK_START_SECURITY.txt

┌──────────────────────────────────────────────────────────────────────────┐
│ EXAMPLE .env FILE                                                        │
└──────────────────────────────────────────────────────────────────────────┘

# Required
NEXT_PUBLIC_STRAPI_URL=https://dashboard.zoeholidays.com
STRAPI_HOST=https://dashboard.zoeholidays.com
NEXT_PUBLIC_STRAPI_TOKEN=abc123def456...
NEXT_PUBLIC_INSTAGRAM_TOKEN=IGAABcd123...
NEXT_PUBLIC_WHATSAPP_NUMBER=201000000000

# Optional - Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-ABC123DEF4

# Optional - Cloudflare (both required if using)
CLOUDFLARE_ZONE_ID=1234567890abcdef...
CLOUDFLARE_API_TOKEN=abcdef1234567890...

┌──────────────────────────────────────────────────────────────────────────┐
│ WORKFLOW                                                                 │
└──────────────────────────────────────────────────────────────────────────┘

First Time Setup:
  1. npm run env:setup
  2. Edit .env with your values
  3. npm run env:check
  4. npm run dev

Before Each Deployment:
  1. npm run env:check
  2. npm run build:safe
  3. npm run security:check
  4. Deploy with confidence!

Regular Maintenance:
  • Weekly: npm run security:audit
  • Monthly: Check for expired tokens
  • Quarterly: Rotate all API keys

╔══════════════════════════════════════════════════════════════════════════╗
║  💡 TIP: Run 'npm run env:check' anytime to validate your configuration  ║
╚══════════════════════════════════════════════════════════════════════════╝
